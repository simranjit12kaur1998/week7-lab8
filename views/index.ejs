<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Goals</title>
    <link rel="stylesheet" href="/styles.css"> <!-- Link to external stylesheet -->
</head>
<body>
    <h1>Goals List</h1>
    <ul>
        <% if (goals.length > 0) { %>
            <li><%= goals[0].title %></li> <!-- Render the title of the first goal -->
        <% } else { %>
            <li>No goals found.</li>
        <% } %>
    </ul>
</body>
</html>